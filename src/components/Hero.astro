<!-- HERO SECTION -->
<section class="hero">
  <div class="hero-content">
    <h1>Velkommen til vores hjemmeside</h1>
    <p>Her er nogle fantastiske oplysninger om os.</p>
    <a href="programme.html" class="btn">Programme</a>
  </div>
  <img src="/img/sakuralogo.svg" alt="Hero billede" />
</section>

<style>
  /* Tilpas størrelsen af billedet til hero-sektionen */
  .hero {
    position: relative;
    background-color: #ffebee;
    width: 100%;
    height: 1000px; /* Juster højden efter behov */
    overflow: hidden;
  }

  .hero img {
    width: 100px;
    float: right;
  }

  /* Centrer indholdet i hero-sektionen */
  .hero-content {
    position: absolute;
    margin-right: 10px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: black;
  }

  .hero-content h1 {
    font-size: 2.5rem;
    margin-bottom: 20px;
  }

  .hero-content p {
    font-size: 1.2rem;
    margin-bottom: 30px;
  }

  .hero-content .btn {
    display: none; /* Skjul knappen som standard */
  }

  /* Responsivt design til mindre skærme */
  @media screen and (max-width: 768px) {
    .hero {
      height: 650px;
    }

    img {
      display: none;
    }

    .hero-content h1 {
      font-size: 2rem;
    }

    .hero-content p {
      font-size: 1rem;
    }

    /* Vis knappen i mobilvisning */
    .hero-content .btn {
      display: inline-block;
      color: blue;
      background-color: hotpink;
      border-radius: 3px;
      padding: 10px;
    }
  }
</style>
